import React from 'react';
import { Button } from 'antd';

const Index = () => {
    return (
        <div>
            index
            <Button type="primary">按钮</Button>
        </div>
    );
};

export default Index;
